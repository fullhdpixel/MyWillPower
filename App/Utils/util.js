//Utility functions

//Turn nested array in flat array
// export function flatten(arr) {
//   return arr.reduce(function(flat, toFlatten) {
//     return flat.concat(
//       Array.isArray(toFlatten) ? flatten(toFlatten) : toFlatten
//     );
//   }, []);
// }
